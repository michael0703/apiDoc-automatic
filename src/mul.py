class Mul:

    def multiply(self, a, b):
        '''
            Args:
                a(int) : integer1 to be multiply
                b(int) : integer2 to be multiply
            Return:
                a * b
        '''

        return a * b